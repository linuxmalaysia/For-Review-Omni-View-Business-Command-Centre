# Render.com static site specification
services:
  - type: static
    name: omni-view-command-centre
    buildCommand: python3 parse_llms_txt.py --generate-all
    staticPublishPath: ./
    headers:
      - path: /*
        name: Cache-Control
        value: max-age=3600
